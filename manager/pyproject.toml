[project]
name = "manager"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.42.2",
    "pandas>=2.3.3",
    "streamlit[auth]>=1.52.0",
]

[dependency-groups]
dev = [
    "watchdog>=6.0.0",
]
